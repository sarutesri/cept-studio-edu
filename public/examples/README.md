# Public examples

Examples teach the direct OpenDSS calculation before the corresponding CEPT
workflow and carry explicit units, assumptions, comparison semantics, and
verification boundaries.

`first_circuit_case.json` is the small typed Case used by the first public
load-flow lesson. It is a demonstrator input: the solver result is produced at
runtime and the example does not claim project or field validation.
